#!/bin/bash

_LEVEL="${1:-3}"
_LEVEL=$((_LEVEL + 1))
_PADDING="　"

for (( i=_LEVEL; i>0; i--));do
    echo "$i"
done \
  | perl -snle '$l="$pad"x$_;$m="$pad"x($num-$_);print $l."（$m　$m）"' -- -num="${_LEVEL}" -pad="${_PADDING}" \
  | sed "s/（${_PADDING}）/${_PADDING}人/"
