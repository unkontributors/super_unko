#!/bin/sh
usage(){
cat << EOF
$0 [arg1] [arg2]
arg1: alternative character of 💩.
arg2: phrase for shout.
EOF
exit 0
}
if [ $# -lt 1 ] ; then usage; fi
unko="$1"; shift
unko.shout ${1+"$@"} | sed "s/💩/$unko/g"
