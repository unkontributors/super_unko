#!/bin/bash
# Get current directory
__THIS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-${(%):-%N}}")"; pwd)"

case ${OSTYPE} in
  darwin*)
    __COW_FILE=unko_mb.cow
    ;;
  linux*)
    __COW_FILE=unko.cow
    ;;
esac

cowsay -f $__THIS_DIR/$__COW_FILE $@
